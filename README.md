# BI-GPT: Business Intelligence Agent

BI-GPT — это агент, который принимает вопросы на естественном языке и переводит их в безопасные SQL-запросы с учётом бизнес-словаря.

## 🚀 Быстрый старт

### Демо-режим (без настройки API ключей)
```bash
# 1. Установка основных зависимостей
pip install fastapi uvicorn pydantic pydantic-settings pyyaml

# 2. Запуск приложения
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 3. Откройте в браузере
# http://localhost:8000
```

### Полная установка (с OpenAI API)
```bash
# 1. Установка всех зависимостей
pip install -r requirements.txt

# 2. Настройка окружения
cp config.env .env
# Отредактируйте .env файл с вашими настройками OpenAI API

# 3. Запуск приложения
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 4. Откройте в браузере
# http://localhost:8000
```

## 🎯 Демо-режим

Приложение автоматически работает в **демо-режиме** без необходимости настройки реального API ключа OpenAI. В этом режиме:

- ✅ Используются предопределенные SQL запросы из `golden_queries.yaml`
- ✅ Поддерживаются все основные типы бизнес-запросов
- ✅ Показывается полный анализ запросов с объяснениями
- ✅ Работает веб-интерфейс с примерами

### Поддерживаемые запросы в демо-режиме:

- **"Прибыль за последние 2 дня"** → SQL для расчета валовой прибыли
- **"Выручка за прошлый месяц"** → SQL для месячной выручки  
- **"Топ 10 товаров по выручке"** → SQL для топ-товаров
- **"Рейтинг магазинов по выручке"** → SQL для рейтинга магазинов
- **"Сравнение регионов по продажам"** → SQL для регионального анализа
- **"Активность клиентов за месяц"** → SQL для клиентской активности
- **"Маржинальность за июль 2024 по регионам"** → SQL для маржинальности
- **"Анализ конверсии заказов по статусам"** → SQL для конверсии

## 📋 Основные возможности

- ✅ Понимание вопросов на естественном языке (русский, казахский, английский)
- ✅ Генерация валидного, безопасного SQL (read-only)
- ✅ Учёт бизнес-словаря (маржа, средний чек, валовая прибыль, SKU, store, region)
- ✅ Возврат агрегатов с понятным объяснением
- ✅ Защита PII данных
- ✅ Логирование и аудит запросов
- ✅ Метрики качества и мониторинг
- ✅ **Демо-режим** для быстрого тестирования без настройки

## 🏗️ Архитектура

```
UI → NL Parser → Retriever → LLM/Demo → Safety → Sandbox → Executor → Verifier → UI + Logs
```

## 🔧 Компоненты

1. **Business Glossary** - источник истины для бизнес-терминов
2. **Schema Retriever** - маппинг терминов к колонкам БД
3. **SQL Generator** - LLM для генерации SQL (или демо-режим)
4. **Safety Layer** - проверка безопасности запросов
5. **Query Sandbox** - выполнение в изолированной среде
6. **Postprocessor** - валидация и объяснение результатов
7. **Audit & Logging** - логирование и мониторинг

## 📊 Примеры использования

### Веб-интерфейс
1. Откройте `http://localhost:8000`
2. Введите вопрос или нажмите на кнопки с примерами
3. Получите SQL с подробным анализом

### API запрос
```bash
curl -X POST "http://localhost:8000/api/v1/query/" \
  -H "Content-Type: application/json" \
  -d '{
    "question": "Прибыль за последние 2 дня",
    "user_id": "demo",
    "user_role": "manager"
  }'
```

### Пример ответа
```json
{
  "request_id": "uuid-here",
  "status": "completed",
  "result": {
    "sql_query": "SELECT DATE(s.order_date) AS day, SUM(s.revenue - s.cogs) AS gross_profit FROM sales s WHERE s.order_date >= current_date - INTERVAL '2 day' AND s.order_date < current_date GROUP BY DATE(s.order_date) ORDER BY day;",
    "execution_time_ms": 150,
    "row_count": 2,
    "columns": ["day", "gross_profit"]
  },
  "explanation": {
    "tables_used": ["sales"],
    "filters_applied": ["s.order_date >= current_date - INTERVAL '2 day'"],
    "business_terms": ["gross_profit"],
    "assumptions": ["Использована текущая дата для временных фильтров"]
  }
}
```

## 🔒 Безопасность

- **Только SELECT запросы** - блокировка DML операций
- **PII защита** - автоматическое обнаружение и блокировка персональных данных
- **Валидация схемы** - доступ только к разрешенным таблицам
- **Ограничения ресурсов** - лимиты на время выполнения и размер результата
- **Аудит** - полное логирование всех запросов

## 📈 Мониторинг

### API метрик
- `/api/v1/metrics/overall` - общие метрики системы
- `/api/v1/metrics/security` - метрики безопасности
- `/api/v1/metrics/performance` - метрики производительности
- `/api/v1/metrics/dashboard` - дашборд с ключевыми показателями

### Ключевые метрики
- **exec_accuracy** - процент успешно выполненных запросов
- **pii_incidents** - количество инцидентов с PII
- **avg_response_time** - среднее время ответа
- **self_service_rate** - процент запросов без участия аналитика

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest tests/

# Тесты с покрытием
pytest tests/ --cov=app

# Тесты golden queries
pytest tests/test_golden_queries.py -v
```

## 📁 Структура проекта

```
BI-GPT/
├── app/                    # Основное приложение
│   ├── api/               # API endpoints
│   │   ├── query.py       # Основной endpoint для запросов
│   │   ├── health.py      # Health check
│   │   └── metrics.py     # Метрики
│   ├── core/              # Основные компоненты (метрики, логирование)
│   ├── models/            # Модели данных
│   ├── services/          # Бизнес-логика
│   │   ├── sql_generator.py    # Генератор SQL (LLM или демо)
│   │   ├── query_executor.py   # Выполнение запросов
│   │   ├── glossary_service.py # Бизнес-словарь
│   │   └── security_service.py # Безопасность
│   ├── static/            # Статические файлы
│   │   └── index.html     # Веб-интерфейс
│   └── main.py            # Точка входа
├── data/                  # Данные
│   ├── business_glossary.yaml  # Бизнес-словарь
│   └── golden_queries.yaml     # Тестовые запросы для демо
├── tests/                 # Тесты
├── requirements.txt       # Зависимости
├── config.py             # Конфигурация
├── config.env            # Пример конфигурации
├── demo.py               # Полная демонстрация
├── simple_demo.py        # Простая демонстрация
└── examples.md           # Примеры использования
```

## 🔧 Конфигурация

### Демо-режим
Приложение автоматически работает в демо-режиме, если:
- Отсутствует файл `.env`
- `OPENAI_API_KEY` не настроен или равен `"your_openai_api_key_here"`

### Полная конфигурация
Основные настройки в `config.env`:

```bash
# База данных
DATABASE_URL=postgresql://user:password@localhost:5432/bi_gpt_db

# OpenAI (для полного режима)
OPENAI_API_KEY=sk-proj-your-actual-openai-api-key-here
OPENAI_MODEL=gpt-4

# Безопасность
MAX_QUERY_ROWS=1000000
QUERY_TIMEOUT_SECONDS=30
MAX_QUERY_COST=1000

# Логирование
LOG_LEVEL=INFO
```

## 📚 Документация

- [Примеры использования](examples.md) - подробные примеры запросов и ответов
- [API документация](http://localhost:8000/docs) - интерактивная документация Swagger
- [Golden queries](data/golden_queries.yaml) - набор тестовых запросов для демо-режима

## 🚀 Развертывание

### Локальная разработка
```bash
# Клонирование репозитория
git clone https://github.com/your-repo/BI-GPT.git
cd BI-GPT

# Установка зависимостей
pip install fastapi uvicorn pydantic pydantic-settings pyyaml structlog

# Запуск в демо-режиме
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### Продакшн
```bash
# Установка всех зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
export OPENAI_API_KEY="your-api-key"
export DATABASE_URL="your-database-url"

# Запуск
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Добавьте тесты для новой функциональности
4. Убедитесь, что все тесты проходят
5. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.

## 🆘 Поддержка

Если у вас возникли проблемы:

1. **Демо-режим не работает** - проверьте, что установлены основные зависимости
2. **API возвращает ошибки** - проверьте логи сервера
3. **Веб-интерфейс не загружается** - убедитесь, что сервер запущен на порту 8000

Для получения помощи создайте issue в репозитории.